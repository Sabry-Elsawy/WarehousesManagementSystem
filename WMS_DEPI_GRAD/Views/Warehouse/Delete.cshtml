@model WMS.DAL.Warehouse

@{
    ViewData["Title"] = "Delete Warehouse";
}

<div class="container-fluid">
    <div class="d-flex justify-content-center mt-5">
        <div class="col-lg-6">
            <div class="card border-0 shadow-sm">
                <div class="card-body p-5 text-center">
                    <div class="mb-4 text-danger">
                        <i class="fas fa-exclamation-triangle fa-4x"></i>
                    </div>
                    <h2 class="fw-bold mb-3">Are you sure?</h2>
                    <p class="text-muted mb-4">
                        You are about to delete the warehouse <strong>@Model.Name</strong> (@Model.Code).
                        <br>This action cannot be undone.
                    </p>

                    <div class="bg-light p-3 rounded mb-4 text-start">
                        <div class="row">
                            <div class="col-6">
                                <small class="text-muted d-block">Capacity</small>
                                <strong>@Model.Capacity</strong>
                            </div>
                            <div class="col-6">
                                <small class="text-muted d-block">Location</small>
                                <strong>@Model.City</strong>
                            </div>
                        </div>
                    </div>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-flex justify-content-center gap-3">
                            <a asp-action="Index" class="btn btn-light px-4">Cancel</a>
                            <button type="submit" class="btn btn-danger px-4">Delete Warehouse</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
